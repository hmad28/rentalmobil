export type Vehicle = {
  name: string;
  image: string;
  selfDrivePrice?: string;
  allInPrice?: string;
  dailyPrice?: string;
};

export type Benefit = {
  title: string;
  description: string;
  image: string;
};
